[project]
name = "okf-tasks"
version = "0.3.0"
description = "An OKF profile and reference implementation for portable task records"
requires-python = ">=3.10"
dependencies = ["PyYAML>=6.0"]
license = "Apache-2.0"

[tool.pytest.ini_options]
testpaths = ["tests"]
